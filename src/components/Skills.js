import React from 'react';

const Skills = () => (
    <section id="skills">
        <h2>Skills</h2>
        <p>React, Node.js, Express, JavaScript, HTML, CSS, etc.</p>
    </section>
);

export default Skills;
